import { Button } from '@/components/common/Button/Button';

export default function PollButton() {
  return (
    <Button variant="solid" buttonType="primary" size="large">
      투표하기
    </Button>
  );
}
